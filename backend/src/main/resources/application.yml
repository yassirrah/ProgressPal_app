spring:
  application:
    name: ProgressPalBackend
  config:
    import: optional:file:./env/db.dev.env[.properties]

  datasource:
    url: jdbc:postgresql://localhost:5432/progresspal
    username: progress
    password: progress

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

logging:
  level:
    org.springframework: INFO

app:
  security:
    allow-header-auth: ${APP_SECURITY_ALLOW_HEADER_AUTH:false}
    jwt:
      secret: ${APP_SECURITY_JWT_SECRET}
