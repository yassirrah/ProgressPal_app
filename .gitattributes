# Default: keep LF in the repo
* text=auto eol=lf

# Windows batch scripts should use CRLF in the working tree
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must stay LF
*.sh text eol=lf
mvnw text eol=lf
